name: Reusable workflow example

on:
  workflow_call:


jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - name: message
      run: echo resuable workflow demo
    - name: Go-Linty
      uses: ArangoGutierrez/GoLinty-Action@go-1.13rc1-alpine
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
